name: 💖 Auto Deploy - Dark AI

on:
  push:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      deploy_message:
        description: 'وصف النشر'
        required: false
        default: '💖 Auto Deploy Dark AI'

permissions:
  contents: write

jobs:
  generate-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 سحب المستودع
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🐍 إعداد Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: 🔧 تشغيل scraper.py لتوليد موقع Dark AI
        run: |
          echo "🎨 جاري توليد موقع Dark AI الفخم..."
          python scraper.py
          echo "✅ تم توليد الموقع في مجلد www/"
      
      - name: 📊 عرض محتويات www
        run: |
          echo "📁 محتويات مجلد www:"
          ls -la www/
          echo ""
          echo "💾 حجم الملفات:"
          du -sh www/
      
      - name: 📋 نسخ index.html للجذر للعرض المباشر
        run: |
          if [ -f "www/index.html" ]; then
            cp www/index.html index.html
            echo "✅ تم نسخ index.html للجذر"
          fi
      
      - name: 🚀 رفع الملفات إلى GitHub
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "💖 Auto Deploy - Dark AI Website [skip ci]"
          commit_author: "Bot 🤖 <actions@github.com>"
          file_pattern: 'www/* index.html'
      
      - name: ✅ تأكيد النشر
        run: |
          echo "╔══════════════════════════════════════╗"
          echo "║  💖 Dark AI Website Generated!     ║"
          echo "║  📁 www/ جاهز للبناء               ║"
          echo "╚══════════════════════════════════════╝"
